<!DOCTYPE html>
<html lang="zh" xml:lang="zh" xmlns="http://www.w3.org/1999/xhtml">

<head>
    <% include partials/head %>

    <title>後台 Dashboard View</title>
    <!-- Custom styles for this template -->
    <link rel="stylesheet" href="/css/console.css">
</head>

<body>

    <h2>《青春最強音》後台 Dashboard View</h2>
    <br/>
    <div class="container" id="dashboard-view">
        <div class="row">
            <div class="col-md-offset-3 col-md-6">
                <table class="table table-bordered" style="table-layout: fixed;">
                    <tbody>
                        <tr>
                            <td colspan="1"></td>
                            <td v-for="ptcp in participants"><h4>{{ ptcp.name }}</h4></td>
                        </tr>
                        <tr>
                            <td><h4>總分</h4></td>
                            <td v-for="(ptcp, ptidx) in participants"><h4>{{ sumByPart(ptidx) }}</h4></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="row">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <td colspan="2"></td>
                        <td v-for="rt in ratings">
                            <h4>{{ rt.name }}</h4>
                        </td>
                        <td><h4>小計</h4></td>
                    </tr>
                    <template v-for="(rd, rdidx) in rounds">
                    <tr v-for="(ptcp, ptidx) in participants">
                        <td v-if="ptidx == 0" rowspan="3"><h3>{{ rd.name }}</h3></td>
                        <td><h4>{{ ptcp.name }}</h4></td>

                        <td v-for="score in scores[rdidx][ptidx]"><h4>{{ score }}</h4></td>

                        <td><h4>{{ sum(scores[rdidx][ptidx]) }}</h4></td>
                    </tr>
                    </template>
                </tbody>
            </table>
        </div>
    </div>
    <script type="text/javascript" src="/js/console.js"></script>
</body>

</html>
